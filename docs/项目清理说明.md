# 项目清理说明

清理目标是减少过期脚本、缓存和重复说明，保留当前运行链路需要的内容。

## 已清理

- 删除 `training/scripts/common.py`，将仍需使用的路径和复制逻辑内联到对应训练脚本。
- 删除 `training/scripts/train_final_model(1650).py`，保留统一的 `train_final_model.py`。
- 删除 Python 缓存目录：
  - `training/scripts/__pycache__/`
  - `backend/python_service/__pycache__/`
- 重写 `docs/` 下说明文档，移除口语化和 AI 式表达。

## 已新增

- `anylabeling_env/`：AnyLabeling 专用虚拟环境。
- 前端 Element Plus 分页式页面结构。
- 图片识别结果框选展示模块。

## 保留原则

- 保留当前前端、Node 后端、Python 推理服务和训练流程仍使用的文件。
- 保留数据处理、伪标注、划分、训练和验证脚本。
- 不删除数据集、模型权重和运行记录。
